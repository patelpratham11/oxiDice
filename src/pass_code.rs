
use crate::config::Configurator;

pub struct pass_code {}

impl pass_code {
    pub fn generate(config: &Configurator) {
        println!("hi");
    }

    // make letter pool

    // generate code 

    // calculate entropy
}